function y = forward_pass(W,b,x,NN)

n_ip = NN{1};                  
n_op = NN{2};                  
n_layers = NN{3};              
n_nodes = NN{4};

for i = 1:n_layers-1
    s{i+1} = W{i}*x + b{i};
    x{i+1} = 1./(1+exp(-s{i+1}));
end